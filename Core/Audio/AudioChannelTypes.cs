namespace XiheFramework.Modules.Audio {
    public enum AudioChannelTypes {
        Master = 0,
        BGM, //background music
        BGS, //environment sound
        CV, //character voice
        SE //sound effect
    }
}