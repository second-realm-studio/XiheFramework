using UnityEngine;

namespace XiheFramework.Utility.Playables.TimeControl {
    public class TimeControlClip : MonoBehaviour {
        // Start is called before the first frame update
        private void Start() { }

        // Update is called once per frame
        private void Update() { }
    }
}